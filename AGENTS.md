# QtVCodec System Agents & Architecture

Tài liệu này mô tả các thành phần chính (Agents) trong hệ thống QtVcodec và cách chúng phối hợp để xử lý nén video.

## Nguyên tắc thiết kế & Hướng dẫn sử dụng (Design Principles & Usage Guidelines)

### 1. Quy tắc quản lý bộ nhớ (Memory Ownership)

### 2. Quy tắc sử dụng `setState()` an toàn

### 3. Quy tắc triển khai `release()` và `onRelease()`

### 4. Quy tắc tương tác đa luồng (Multithreading)

### 5. Quy tắc xử lý mã lỗi
